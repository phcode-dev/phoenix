// Fixture for the LSP hover quick-action tests: a symbol with a definition (line 1) and usages.
function greetUser(userName) {
    return "Hi " + userName;
}

greetUser("alpha");
greetUser("bravo");
