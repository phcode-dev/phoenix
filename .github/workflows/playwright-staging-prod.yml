name: On pull request on staging or prod branch, Chromium and firefox (Win/Mac/Linux) run full tests suite

on:
  pull_request:
    branches: [ staging, prod ]

jobs:
  playwrightChromiumLinux:
    timeout-minutes: 90
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build phoenix
        run: |
          npm run build
          npm run release:prod
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright unit tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumUnit
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npm run testChromiumDist

      - name: Run Playwright integration tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumIntegration
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=integration npm run testChromiumDist

      - name: Run Playwright mainview tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumMainview
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=mainview npm run testChromiumDist

      - name: Run Playwright livepreview tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumLivepreview
        continue-on-error: true
        with:
          timeout_minutes: 15
          max_attempts: 3
          command: npx cross-env TEST_ENV=livepreview npm run testChromiumDist

      - name: Run Playwright LegacyInteg tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumLegacyInteg
        continue-on-error: true
        with:
          timeout_minutes: 20
          max_attempts: 3
          command: npx cross-env TEST_ENV=LegacyInteg npm run testChromiumDist

      - name: Fail on test runs failed in Chromium Linux (staging/prod)
        if: steps.chromiumUnit.outcome == 'failure' || steps.chromiumIntegration.outcome == 'failure' || steps.chromiumMainview.outcome == 'failure' || steps.chromiumLivepreview.outcome == 'failure' || steps.chromiumLegacyInteg.outcome == 'failure'
        run: |
          echo "Chromium Linux (staging/prod) Playwright tests failed, marking step as failed"
          echo "Failed tests:"
          if [ "${{ steps.chromiumUnit.outcome }}" == "failure" ]; then
            echo "- Run Playwright unit tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumIntegration.outcome }}" == "failure" ]; then
            echo "- Run Playwright integration tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumMainview.outcome }}" == "failure" ]; then
            echo "- Run Playwright mainview tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumLivepreview.outcome }}" == "failure" ]; then
            echo "- Run Playwright livepreview tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumLegacyInteg.outcome }}" == "failure" ]; then
            echo "- Run Playwright LegacyInteg tests in Chromium for prod stage"
          fi
          exit 1

  playwrightChromiumWindows:
    timeout-minutes: 90
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build phoenix
        run: |
          npm run build
          npm run release:prod
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright unit tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumUnit
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npm run testChromiumDist

      - name: Run Playwright integration tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumIntegration
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=integration npm run testChromiumDist

      - name: Run Playwright mainview tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumMainview
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=mainview npm run testChromiumDist

      - name: Run Playwright livepreview tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumLivepreview
        continue-on-error: true
        with:
          timeout_minutes: 15
          max_attempts: 3
          command: npx cross-env TEST_ENV=livepreview npm run testChromiumDist

      - name: Run Playwright LegacyInteg tests in Chromium for prod stage
        uses: nick-fields/retry@v2
        id: chromiumLegacyInteg
        continue-on-error: true
        with:
          timeout_minutes: 30
          max_attempts: 3
          command: npx cross-env TEST_ENV=LegacyInteg npm run testChromiumDist

      - name: Fail on test runs failed in Chromium Windows (staging/prod)
        if: steps.chromiumUnit.outcome == 'failure' || steps.chromiumIntegration.outcome == 'failure' || steps.chromiumMainview.outcome == 'failure' || steps.chromiumLivepreview.outcome == 'failure' || steps.chromiumLegacyInteg.outcome == 'failure'
        shell: bash
        run: |
          echo "Chromium Windows (staging/prod) Playwright tests failed, marking step as failed"
          echo "Failed tests:"
          if [ "${{ steps.chromiumUnit.outcome }}" == "failure" ]; then
            echo "- Run Playwright unit tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumIntegration.outcome }}" == "failure" ]; then
            echo "- Run Playwright integration tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumMainview.outcome }}" == "failure" ]; then
            echo "- Run Playwright mainview tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumLivepreview.outcome }}" == "failure" ]; then
            echo "- Run Playwright livepreview tests in Chromium for prod stage"
          fi
          if [ "${{ steps.chromiumLegacyInteg.outcome }}" == "failure" ]; then
            echo "- Run Playwright LegacyInteg tests in Chromium for prod stage"
          fi
          exit 1

  playwrightChromiumMacos:
    timeout-minutes: 90
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build phoenix
        run: |
          npm run build
          npm run release:prod
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright unit tests in Chromium
        uses: nick-fields/retry@v2
        id: chromiumUnit
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npm run testChromiumDist

      - name: Run Playwright integration tests in Chromium
        uses: nick-fields/retry@v2
        id: chromiumIntegration
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=integration npm run testChromiumDist

      - name: Run Playwright mainview tests in Chromium
        uses: nick-fields/retry@v2
        id: chromiumMainview
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=mainview npm run testChromiumDist

      - name: Run Playwright livepreview tests in Chromium
        uses: nick-fields/retry@v2
        id: chromiumLivepreview
        continue-on-error: true
        with:
          timeout_minutes: 15
          max_attempts: 3
          command: npx cross-env TEST_ENV=livepreview npm run testChromiumDist

      - name: Run Playwright LegacyInteg tests in Chromium
        uses: nick-fields/retry@v2
        id: chromiumLegacyInteg
        continue-on-error: true
        with:
          timeout_minutes: 20
          max_attempts: 3
          command: npx cross-env TEST_ENV=LegacyInteg npm run testChromiumDist

      - name: Fail on test runs failed in Chromium macOS (staging/prod)
        if: steps.chromiumUnit.outcome == 'failure' || steps.chromiumIntegration.outcome == 'failure' || steps.chromiumMainview.outcome == 'failure' || steps.chromiumLivepreview.outcome == 'failure' || steps.chromiumLegacyInteg.outcome == 'failure'
        run: |
          echo "Chromium macOS (staging/prod) Playwright tests failed, marking step as failed"
          echo "Failed tests:"
          if [ "${{ steps.chromiumUnit.outcome }}" == "failure" ]; then
            echo "- Run Playwright unit tests in Chromium"
          fi
          if [ "${{ steps.chromiumIntegration.outcome }}" == "failure" ]; then
            echo "- Run Playwright integration tests in Chromium"
          fi
          if [ "${{ steps.chromiumMainview.outcome }}" == "failure" ]; then
            echo "- Run Playwright mainview tests in Chromium"
          fi
          if [ "${{ steps.chromiumLivepreview.outcome }}" == "failure" ]; then
            echo "- Run Playwright livepreview tests in Chromium"
          fi
          if [ "${{ steps.chromiumLegacyInteg.outcome }}" == "failure" ]; then
            echo "- Run Playwright LegacyInteg tests in Chromium"
          fi
          exit 1

  playwrightFirefoxLinux:
    timeout-minutes: 90
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build phoenix
        run: |
          npm run build
          npm run release:prod
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright unit tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxUnit
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npm run testFirefoxDist

      - name: Run Playwright integration tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxIntegration
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=integration npm run testFirefoxDist

      - name: Run Playwright mainview tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxMainview
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=mainview npm run testFirefoxDist

      - name: Run Playwright livepreview tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxLivepreview
        continue-on-error: true
        with:
          timeout_minutes: 15
          max_attempts: 3
          command: npx cross-env TEST_ENV=livepreview npm run testFirefoxDist

      - name: Run Playwright LegacyInteg tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxLegacyInteg
        continue-on-error: true
        with:
          timeout_minutes: 20
          max_attempts: 3
          command: npx cross-env TEST_ENV=LegacyInteg npm run testFirefoxDist

      - name: Fail on test runs failed in Firefox Linux (staging/prod)
        if: steps.firefoxUnit.outcome == 'failure' || steps.firefoxIntegration.outcome == 'failure' || steps.firefoxMainview.outcome == 'failure' || steps.firefoxLivepreview.outcome == 'failure' || steps.firefoxLegacyInteg.outcome == 'failure'
        run: |
          echo "Firefox Linux (staging/prod) Playwright tests failed, marking step as failed"
          echo "Failed tests:"
          if [ "${{ steps.firefoxUnit.outcome }}" == "failure" ]; then
            echo "- Run Playwright unit tests in Firefox"
          fi
          if [ "${{ steps.firefoxIntegration.outcome }}" == "failure" ]; then
            echo "- Run Playwright integration tests in Firefox"
          fi
          if [ "${{ steps.firefoxMainview.outcome }}" == "failure" ]; then
            echo "- Run Playwright mainview tests in Firefox"
          fi
          if [ "${{ steps.firefoxLivepreview.outcome }}" == "failure" ]; then
            echo "- Run Playwright livepreview tests in Firefox"
          fi
          if [ "${{ steps.firefoxLegacyInteg.outcome }}" == "failure" ]; then
            echo "- Run Playwright LegacyInteg tests in Firefox"
          fi
          exit 1

  playwrightFirefoxWindows:
    timeout-minutes: 90
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build phoenix
        run: |
          npm run build
          npm run release:prod
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright unit tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxUnit
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npm run testFirefoxDist

      - name: Run Playwright integration tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxIntegration
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=integration npm run testFirefoxDist

      - name: Run Playwright mainview tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxMainview
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=mainview npm run testFirefoxDist

      - name: Run Playwright livepreview tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxLivepreview
        continue-on-error: true
        with:
          timeout_minutes: 15
          max_attempts: 3
          command: npx cross-env TEST_ENV=livepreview npm run testFirefoxDist

      - name: Run Playwright LegacyInteg tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxLegacyInteg
        continue-on-error: true
        with:
          timeout_minutes: 20
          max_attempts: 3
          command: npx cross-env TEST_ENV=LegacyInteg npm run testFirefoxDist

      - name: Fail on test runs failed in Firefox Windows (staging/prod)
        if: steps.firefoxUnit.outcome == 'failure' || steps.firefoxIntegration.outcome == 'failure' || steps.firefoxMainview.outcome == 'failure' || steps.firefoxLivepreview.outcome == 'failure' || steps.firefoxLegacyInteg.outcome == 'failure'
        shell: bash
        run: |
          echo "Firefox Windows (staging/prod) Playwright tests failed, marking step as failed"
          echo "Failed tests:"
          if [ "${{ steps.firefoxUnit.outcome }}" == "failure" ]; then
            echo "- Run Playwright unit tests in Firefox"
          fi
          if [ "${{ steps.firefoxIntegration.outcome }}" == "failure" ]; then
            echo "- Run Playwright integration tests in Firefox"
          fi
          if [ "${{ steps.firefoxMainview.outcome }}" == "failure" ]; then
            echo "- Run Playwright mainview tests in Firefox"
          fi
          if [ "${{ steps.firefoxLivepreview.outcome }}" == "failure" ]; then
            echo "- Run Playwright livepreview tests in Firefox"
          fi
          if [ "${{ steps.firefoxLegacyInteg.outcome }}" == "failure" ]; then
            echo "- Run Playwright LegacyInteg tests in Firefox"
          fi
          exit 1

  playwrightFirefoxMacos:
    timeout-minutes: 90
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v3
      - name: setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build phoenix
        run: |
          npm run build
          npm run release:prod
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright unit tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxUnit
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npm run testFirefoxDist

      - name: Run Playwright integration tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxIntegration
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=integration npm run testFirefoxDist

      - name: Run Playwright mainview tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxMainview
        continue-on-error: true
        with:
          timeout_minutes: 12
          max_attempts: 3
          command: npx cross-env TEST_ENV=mainview npm run testFirefoxDist

      - name: Run Playwright livepreview tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxLivepreview
        continue-on-error: true
        with:
          timeout_minutes: 15
          max_attempts: 3
          command: npx cross-env TEST_ENV=livepreview npm run testFirefoxDist

      - name: Run Playwright LegacyInteg tests in Firefox
        uses: nick-fields/retry@v2
        id: firefoxLegacyInteg
        continue-on-error: true
        with:
          timeout_minutes: 20
          max_attempts: 3
          command: npx cross-env TEST_ENV=LegacyInteg npm run testFirefoxDist

      - name: Fail on test runs failed in Firefox macOS (staging/prod)
        if: steps.firefoxUnit.outcome == 'failure' || steps.firefoxIntegration.outcome == 'failure' || steps.firefoxMainview.outcome == 'failure' || steps.firefoxLivepreview.outcome == 'failure' || steps.firefoxLegacyInteg.outcome == 'failure'
        run: |
          echo "Firefox macOS (staging/prod) Playwright tests failed, marking step as failed"
          echo "Failed tests:"
          if [ "${{ steps.firefoxUnit.outcome }}" == "failure" ]; then
            echo "- Run Playwright unit tests in Firefox"
          fi
          if [ "${{ steps.firefoxIntegration.outcome }}" == "failure" ]; then
            echo "- Run Playwright integration tests in Firefox"
          fi
          if [ "${{ steps.firefoxMainview.outcome }}" == "failure" ]; then
            echo "- Run Playwright mainview tests in Firefox"
          fi
          if [ "${{ steps.firefoxLivepreview.outcome }}" == "failure" ]; then
            echo "- Run Playwright livepreview tests in Firefox"
          fi
          if [ "${{ steps.firefoxLegacyInteg.outcome }}" == "failure" ]; then
            echo "- Run Playwright LegacyInteg tests in Firefox"
          fi
          exit 1
